<% flash.each do |type, msg| %>
  <%= flash_message(type, msg) %>
<% end %>

<script>
  window.onload = function(){
    let message = document.getElementById('flash_message')
    if ( message != null ) {
      message.onclick = function(){
         message.remove()
      }
    }
  }
</script>