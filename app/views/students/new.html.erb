<h1>Sign Up</h1>
<br><br>
<%= form_for @student do |f| %>
  <p>Name:<br> <%= f.text_field :name %></p>
  <p>Email:<br> <%= f.email_field :email %></p>
  <%= f.submit "Create", class: "hover:bg-teal-500 text-gray-700 font-semibold hover:text-white py-2 px-4 border border-gray-500 rounded my-2" %> 
<% end %>